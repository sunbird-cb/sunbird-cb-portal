import { PipeFilterV2Pipe } from './pipe-filter-v2.pipe'

describe('PipeFilterV2Pipe', () => {
  it('create an instance', () => {
    const pipe = new PipeFilterV2Pipe()
    expect(pipe).toBeTruthy()
  })
})
