import { PipeFilterV3Pipe } from './pipe-filter-v3.pipe'

describe('PipeFilterV3Pipe', () => {
  it('create an instance', () => {
    const pipe = new PipeFilterV3Pipe()
    expect(pipe).toBeTruthy()
  })
})
